spring:
  datasource:
    url: jdbc:h2:mem:gestao;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
app:
  jwt:
    segredo: test-secret-with-at-least-thirty-two-bytes
    minutosExpiracao: 5
